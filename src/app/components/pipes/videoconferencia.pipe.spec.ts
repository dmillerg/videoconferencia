import { VideoconferenciaPipe } from './videoconferencia.pipe';

describe('VideoconferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoconferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
